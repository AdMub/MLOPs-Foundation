docker build -t <imagename> .
docker run -d -p 5000:5000 myhelloapp
docker login
username
password

docker tag myhelloapp   username/reponame or imagename (admub/projecthellodemo)
docker push admub/projecthellodemo


# Pulling of Virtual Machine
docker run -it ubuntu  bin/sh


# Pulling of container
docker run -p 8080:8080 jenkins/jenkins:lts

# Run Isolated System by creating another container
docker run --name admubcontainer -it ubuntu /bin/bash (CMD or Powershell)


# To check the change difference 
docker diff admubcontainer

# Create another image from the container
docker commit admubcontainer updatedimage
